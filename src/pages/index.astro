---
import BaseLayout from "../layouts/BaseLayout.astro";
import HeroFull from "../components/home/HeroFull.astro";
import Description from "../components/home/Description.astro";
import AboutUs from "../components/home/AboutUs.astro";
import Parallax from "../components/home/Parallax.astro";
import settings from "../content/ustawienia.json";

const { site } = Astro;
const baseUrl = site ? site.toString().replace(/\/$/, "") : "";
const siteUrl = baseUrl ? `${baseUrl}/` : "/";
const siteLogo = settings.logo ? (baseUrl ? `${baseUrl}${settings.logo}` : settings.logo) : "";
---

<BaseLayout
  title="Meble z litego drewna"
  description="Oferujemy meble z litego drewna, które łączą w sobie naturalne piękno i trwałość. Nasze produkty są ręcznie wykonane z najwyższej jakości drewna, co gwarantuje unikalny charakter każdego mebla."
  showHero={false}
>
  <div itemscope itemtype="https://schema.org/WebSite">
    <meta itemprop="name" content={settings.siteTitle} />
    <meta itemprop="url" content={siteUrl} />
    <div itemprop="publisher" itemscope itemtype="https://schema.org/Organization">
      <meta itemprop="name" content={settings.siteTitle} />
      {siteLogo ? <meta itemprop="logo" content={siteLogo} /> : null}
    </div>
  </div>
	<HeroFull />
  <Description />
  <AboutUs />
  <Parallax />

</BaseLayout>
