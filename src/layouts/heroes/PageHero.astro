---
const { title, lead, meta } = Astro.props;
const { site, url } = Astro;
const baseUrl = site ? site.toString().replace(/\/$/, "") : "";
const homeUrl = baseUrl ? `${baseUrl}/` : "/";
const currentUrl = baseUrl ? new URL(url.pathname, baseUrl).toString() : url.pathname;
---

<section class="relative">
    <div class="relative max-w-4xl mx-auto pb-8 pt-28 md:pb-16 md:pt-36 text-center z-10">
      <nav
        class="text-[8px] md:text-[10px] uppercase tracking-[0.1em] text-gray-400 mb-3 md:mb-6 font-semibold"
        aria-label="Breadcrumb"
        itemscope
        itemtype="https://schema.org/BreadcrumbList"
      >
        <span itemprop="itemListElement" itemscope itemtype="https://schema.org/ListItem">
          <a href={homeUrl} itemprop="item" class="hover:text-black">
            <span itemprop="name">Główna</span>
          </a>
          <meta itemprop="position" content="1" />
        </span>
        <span class="text-[var(--accent)]"> > </span>
        <span itemprop="itemListElement" itemscope itemtype="https://schema.org/ListItem">
          <meta itemprop="item" content={currentUrl} />
          <span class="text-black-80" itemprop="name">
            {title}
          </span>
          <meta itemprop="position" content="2" />
        </span>
      </nav>
            <h2 class="text-[9vw] md:text-[4rem] lg:text-[5rem] xl:text-[6rem] tracking-tighter text-black uppercase split-chars">
               {title}
            </h2>
            <p class="font-decorative text-[#C5A47E] text-[6vw] md:text-[4rem] lg:text-[5rem] xl:text-[6rem] absolute top-1/2 right-0 translate-y-12 split-letters">
                Furniture design
            </p>
    </div>
</section>

